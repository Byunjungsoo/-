// Casting 데이터타입을 다른데이터 타입으로 컨버팅


public class Casting {

	public static void main(String[] args) {
		// ex) 1.1; ,1;
		double a = 1.1;   //실수이기 때문에 double 사 용
		double b = 1;     //1은 정수이지만 int 대신에 double을 사용해도 잃어버리는게 없기 때문에 실수에서 사용하는 double을 사용해도 무방
	System.out.println(b);
		
	// int = 1.1; 인경우

	 //    int c = 1.1;   int는 정수를 나타내기 때문에 1.1은 불가능한데 그러면 빨간 밑줄이 나오는데 여기서 1) Add  cast to 'int' 를 클릭하면 1.1은 실수이지만 int 데이터타입에서 쓸수있게 컨버트 해준다
	// ex)  (int), (double)  형 변환 기억
	
	int c = (int) 1.1;
	System.out.println(c);
	
	// 정수 1로 나타남 1.1에서 0.1을 손실시키고
	
	//   	int d = 1.1;  2) change type of 'd' to 'double'   로 바꾸
	 //ex)
	
	double d = 1.1;
	System.out.println(d);
	
	
	// 1 to string 1을 숫자가 아닌 문자열로 나타내
	
	String f = Integer.toString(1);
	System.out.println(f);
	// System.out.println(f.getClass()); 어떤데이터 타입을 가지고 있는지 알려주
	}

}
//  java int to string casting 구글링
